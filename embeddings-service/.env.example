# Shared secret — must match backend/.env's EMBEDDINGS_API_KEY. Leave blank
# only for local dev (auth is then skipped).
EMBEDDINGS_API_KEY=

# Must match scripts/seed/04_embed.py's EMBED_MODEL_ID.
EMBED_MODEL_ID=intfloat/multilingual-e5-base
EMBED_MAX_LENGTH=512
EMBED_MAX_BATCH=32
